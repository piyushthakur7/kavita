"use client";

import React, { useState } from 'react';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import { ArrowLeft, ImagePlus, Save } from 'lucide-react';
import Logo from '@/components/Logo';
import TiptapEditor from '@/components/admin/TiptapEditor';

export default function CreateBlogPage() {
  const router = useRouter();
  const [title, setTitle] = useState(''); const [excerpt, setExcerpt] = useState(''); const [content, setContent] = useState('');
  const [category, setCategory] = useState('Wellness'); const [coverImage, setCoverImage] = useState(''); const [published, setPublished] = useState(true);
  const [uploading, setUploading] = useState(false); const [saving, setSaving] = useState(false); const [error, setError] = useState('');
  async function uploadImage(event) { const file = event.target.files?.[0]; if (!file) return; try { setUploading(true); setError(''); const formData = new FormData(); formData.append('file', file); const response = await fetch('/api/upload', { method: 'POST', body: formData }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'Image upload failed.'); setCoverImage(data.url); } catch (err) { setError(err.message); } finally { setUploading(false); } }
  async function savePost(event) { event.preventDefault(); if (!title || !excerpt || !content || !coverImage) { setError('Add a title, summary, cover image, and article before saving.'); return; } try { setSaving(true); setError(''); const response = await fetch('/api/blogs', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ title, excerpt, content, coverImage, category, published }) }); const data = await response.json(); if (!response.ok) throw new Error(data.error || 'Could not save the post.'); router.push('/blog/admin/dashboard'); router.refresh(); } catch (err) { setError(err.message); } finally { setSaving(false); } }
  return <main className="min-h-screen bg-slate-50 pb-16 text-slate-900"><header className="border-b border-slate-200 bg-white px-5 py-4 shadow-sm"><div className="mx-auto flex max-w-6xl items-center justify-between"><Link href="/blog/admin/dashboard" className="inline-flex items-center gap-2 text-sm font-medium text-slate-600 hover:text-slate-900"><ArrowLeft size={17} />All posts</Link><Logo /></div></header><form onSubmit={savePost} className="mx-auto max-w-6xl px-5 py-10"><div className="mb-8"><p className="text-sm font-medium text-fuchsia-700">New post</p><h1 className="mt-1 font-serif-display text-4xl">Write a blog post</h1><p className="mt-2 text-sm text-slate-600">Add the four required details, then publish or save as a draft.</p></div>{error && <div className="mb-6 rounded-lg border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}<div className="grid gap-8 lg:grid-cols-[1fr_320px]"><div className="space-y-6"><div className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><label className="block text-sm font-semibold">Title <span className="text-red-600">*</span></label><input required value={title} onChange={(e) => setTitle(e.target.value)} placeholder="Example: How to manage stress at work" className="mt-2 w-full rounded-lg border border-slate-300 px-3 py-2.5 text-sm outline-none focus:border-fuchsia-600 focus:ring-2 focus:ring-fuchsia-100" /><label className="mt-5 block text-sm font-semibold">Short summary <span className="text-red-600">*</span></label><textarea required value={excerpt} onChange={(e) => setExcerpt(e.target.value)} rows={3} placeholder="A short introduction shown on the blog page." className="mt-2 w-full resize-y rounded-lg border border-slate-300 px-3 py-2.5 text-sm outline-none focus:border-fuchsia-600 focus:ring-2 focus:ring-fuchsia-100" /></div><div className="rounded-xl border border-slate-200 bg-white p-6 shadow-sm"><label className="mb-2 block text-sm font-semibold">Article <span className="text-red-600">*</span></label><TiptapEditor value={content} onChange={setContent} /></div></div><aside className="space-y-6 lg:sticky lg:top-6 lg:self-start"><div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><button type="submit" disabled={saving || uploading} className="flex w-full items-center justify-center gap-2 rounded-lg bg-fuchsia-700 px-4 py-3 text-sm font-semibold text-white hover:bg-fuchsia-800 disabled:opacity-60"><Save size={16} />{saving ? 'Saving…' : published ? 'Publish post' : 'Save draft'}</button><Link href="/blog/admin/dashboard" className="mt-3 block rounded-lg border border-slate-300 px-4 py-2.5 text-center text-sm font-medium text-slate-700 hover:bg-slate-100">Cancel</Link></div><div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><label className="block text-sm font-semibold">Cover image <span className="text-red-600">*</span></label>{coverImage ? <><img src={coverImage} alt="Cover preview" className="mt-3 h-40 w-full rounded-lg border border-slate-200 object-cover" /><button type="button" onClick={() => setCoverImage('')} className="mt-2 text-sm font-medium text-red-600 hover:underline">Remove image</button></> : <label className="mt-3 flex h-36 cursor-pointer flex-col items-center justify-center rounded-lg border-2 border-dashed border-slate-300 bg-slate-50 text-sm text-slate-600 hover:border-fuchsia-500 hover:bg-fuchsia-50"><ImagePlus size={24} /><span className="mt-2">{uploading ? 'Uploading image…' : 'Choose an image'}</span><input type="file" accept="image/*" onChange={uploadImage} disabled={uploading} className="hidden" /></label>}</div><div className="rounded-xl border border-slate-200 bg-white p-5 shadow-sm"><label className="block text-sm font-semibold">Category</label><select value={category} onChange={(e) => setCategory(e.target.value)} className="mt-2 w-full rounded-lg border border-slate-300 bg-white px-3 py-2.5 text-sm outline-none focus:border-fuchsia-600">{['Wellness', 'Mindset', 'Anxiety', 'Speaker Series', 'Clinical'].map((item) => <option key={item}>{item}</option>)}</select><label className="mt-5 flex cursor-pointer items-center justify-between gap-3 text-sm font-medium"><span>Publish immediately</span><input type="checkbox" checked={published} onChange={(e) => setPublished(e.target.checked)} className="h-4 w-4 accent-fuchsia-700" /></label></div></aside></div></form></main>;
}
